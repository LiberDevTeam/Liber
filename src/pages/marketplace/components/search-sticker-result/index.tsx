import React, { memo } from 'react';

interface Props {
  searchText: string;
}

export const SearchStickerResult: React.FC<Props> = memo(
  function SearchStickerResult() {
    return <>SearchStickerResult</>;
  }
);
