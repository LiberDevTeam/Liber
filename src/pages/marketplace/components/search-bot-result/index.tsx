import React, { memo } from 'react';

interface Props {
  searchText: string;
}

export const SearchBotResult: React.FC<Props> = memo(
  function SearchBotResult() {
    return <>SearchBotResult</>;
  }
);
