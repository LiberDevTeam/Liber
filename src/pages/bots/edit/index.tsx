import React, { memo } from 'react';
import styled from 'styled-components';

const Root = styled.div``;

interface Props {}

export const BotEditPage: React.FC<Props> = memo(function BotEditPage({}) {
  return <Root>BotEditPage</Root>;
});
